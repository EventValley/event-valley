type Query
type Mutation

enum OrderDirection {
	asc
	desc
}

directive @hasRole(role: String!) on FIELD_DEFINITION

input IDOperators {
	equals: ID
	not: ID
	in: [ID!]
	notIn: [ID!]
	contains: ID
	search: ID
}

input StringOperators {
	equals: String
	not: String
	in: [String!]
	notIn: [String!]
	contains: String
	search: String
}

input DateOperators {
	equals: String
	lt: String
	gt: String
}
