type EventBannedUser {
	id: ID!
	eventId: String!
	event: Event
	userId: String!
	user: User
	createdAt: String!
	modifiedAt: String!
}

extend type Query {
	eventBannedUsers: [EventBannedUser]! @hasRole(role: "Organizer")
}
